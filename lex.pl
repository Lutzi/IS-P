% Autor:
% Datum: 20.10.2016

% lex(wort,semantik,wortart,numerus)
lex(wer,_,ip,_).
lex(ist,_,ist,s).
lex(sind,_,ip,p).
lex(der,_,art,s).
lex(von,_,prep,_).

lex(vater,ist_vater_von,n,s).

lex(Name,Name,name,s).

